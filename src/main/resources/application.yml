# Khai báo port
server: 
  port: 8080
  
# Connect SQL Server
spring:
  datasource:
    url: jdbc:sqlserver://localhost;databaseName=DuAnTotNghiep;encrypt=true;trustServerCertificate=true;
    username: sa
    password: 123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update # Hoặc create, none tùy theo nhu cầu
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect # Đảm bảo cung cấp dialect phù hợp
  mail:
    host: smtp.gmail.com
    port: 587
    username: thanh02062004@gmail.com
    password: mjnh dmor vzdx bwrg
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB # Kích thước tối đa cho tệp đơn
      max-request-size: 10MB # Kích thước tối đa cho toàn bộ yêu cầu
