# Khai báo port
server:
  port: 8080

# Connect SQL Server
spring:
  datasource:
    url: jdbc:sqlserver://localhost;databaseName=DuAnTotNghiep2;encrypt=true;trustServerCertificate=true;
    username: sa
    password: songlong
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update # Hoặc create, none tùy theo nhu cầu
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect # Đảm bảo cung cấp dialect phù hợp

  mail:
    host: smtp.gmail.com
    port: 587
    username: quangkmps30913@fpt.edu.vn
    password: xtnsbvffxhqecfny
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB

  web:
    resources:
      static-locations: classpath:/static/,file:images/uploads/
