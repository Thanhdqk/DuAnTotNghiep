import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const voucherReducer = createSlice({
  name: 'voucherReducer',
  initialState,
  reducers: {}
});

export const {} = voucherReducer.actions

export default voucherReducer.reducer